on:
  push:
    branches:
      - master
      - main
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master

      - name: List output files
        run: ls

      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@2.0.0
        env:
          FTP_SERVER: 154.12.237.134
          FTP_USERNAME: mity
          FTP_PASSWORD: NFAxZUX2M6
          LOCAL_DIR: build
          METHOD: sftp
          PORT: 22
          ARGS: '-o ConnectTimeout=5'
          server-dir: /var/www/portfolio.mity.dev/
